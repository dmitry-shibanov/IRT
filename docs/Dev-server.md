Before working with dev server you need to accomplish all pre-requirements from [README.md](../README.md) file.

### Start dev server:
1. Install all required dependencies: `npm ci`
2. To Start web server: `npm run start:dev`